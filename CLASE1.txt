SELECT * FROM DBA_USERS;
SELECT * FROM DBA_TABLESPACES;
SELECT * FROM DBA_DATA_FILES;

GRANT SELECT ON DBA_DATA_FILES TO ROL_DAW;

SELECT * FROM USER_TABLES;
SELECT * FROM DEPART;

EMPLE
DEPART
;

SELECT EMPLE.DEPT_NO,EMPLE.APELLIDO, EMPLE.SALARIO FROM EMPLE;

SELECT EMPLE.APELLIDO , EMPLE.SALARIO , EMPLE.COMISION
  FROM EMPLE;
  
SELECT DEPT_NO
  FROM EMPLE; 
SELECT ALL DEPT_NO
  FROM EMPLE;  
  
SELECT APELLIDO, SALARIO, COMISION, SALARIO + COMISION AS "TOTAL"
  FROM EMPLE;
  
 SELECT APELLIDO, SALARIO, COMISION, SALARIO + COMISION  "TOTAL"
  FROM EMPLE; 
  
 SELECT APELLIDO, SALARIO, COMISION, SALARIO + COMISION AS TOTAL
  FROM EMPLE; 
  
 SELECT APELLIDO, SALARIO, COMISION, SALARIO + COMISION  TOTAL
  FROM EMPLE; 
  
 SELECT APELLIDO, SALARIO, COMISION, SALARIO + COMISION AS "TOTAL SUELDO"
  FROM EMPLE; 
  
  -- CASCA
 SELECT APELLIDO, SALARIO, COMISION, SALARIO + COMISION AS TOTAL SUELDO
  FROM EMPLE; 
  
SELECT APELLIDO, SALARIO, COMISION,  NVL(SALARIO,0) + NVL(COMISION,0)  AS "TOTAL"
  FROM EMPLE;  
  
  
  
 SELECT DISTINCT DEPT_NO
   FROM EMPLE;
 SELECT DISTINCT OFICIO
   FROM EMPLE;  
   
  
SELECT * FROM NOTAS_ALUMNOS;

SELECT DISTINCT NOTA2
  FROM NOTAS_ALUMNOS;
  
SELECT NOMBRE_ALUMNO, NOTA1, NOTA2, NOTA3, (NOTA1 + NOTA2 + NOTA3) / 3 AS MEDIA
  FROM NOTAS_ALUMNOS;
  
SELECT NOMBRE_ALUMNO, NOTA1, NOTA2, NOTA3, (NVL(NOTA1,0) + 
                                            NVL(NOTA2,0) + 
                                            NVL(NOTA3,0) 
                                            ) / 3 AS MEDIAS
  FROM NOTAS_ALUMNOS;    



